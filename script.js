 function calculateArea() {
            // Get the radius from the user usin the prompt() function
            let radius = prompt("Enter the radius of the circle:");
return(`The area of the circle with radius ${radius} is ${3.14*(radius*radius)}`);                
}
alert(calculateArea());
